#include <stdio.h>
/**
 * main - prints the alphabet in lowercase and uppercase
(*
* Return: 0 on success
*/

int main(void)
{
  
    int ch;

    for (ch = 'a'; letter <= 'z'; ch++)
      putchar(ch);

    for (ch = 'A'; ch <= 'Z'; letter++)
      putchar(ch);

    putchar('\n');

    return (0);
  
}
