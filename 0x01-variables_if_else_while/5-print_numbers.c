#include <stdio.h>
/**
 * main - Prints the numbers starting in 0 in base 10.
 *
 * Return: 0.
 */
int main(void)
{
int digit;
for (digit = '0'; digit <= '9'; digit++)
printf("%d",digit);
printf('\n');
return (0);
}
