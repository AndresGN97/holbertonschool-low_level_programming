#include <stdio.h>
/**
 * main - Prints the numbers from 0 to 10.
 *
 * Return: Always 0.
 */
int main(void)
{
int digit;
for (digit = '0'; digit <= '9'; digit++)
printf("%d",digit);
printf('\n');
return (0);
}
